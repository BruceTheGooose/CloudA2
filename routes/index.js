var formSubmit = document.getElementById('twitter-search-form'); // Get location form
if(formSubmit){
  formSubmit.addEventListener('submit', usersInput);
}

function usersInput() {
  //get users input and send back through socket
}
